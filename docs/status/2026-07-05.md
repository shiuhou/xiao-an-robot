# 2026-07-05 Base Mic / OpenClaw / Robot Care Demo Status

## Runtime Used

Base station server:

```bash
XIAOAN_CONTROL_TTS_STREAM=1 \
XIAOAN_TTS_TARGET_PEAK=900 \
XIAOAN_TTS_COMMAND="/home/zzyzsrsyh/.openclaw/workspace-xiaoan-builder/xiao-an-robot/.venv/bin/python /home/zzyzsrsyh/.openclaw/workspace-xiaoan-builder/xiao-an-robot/runtime/tts_probe/edge_tts_to_wav.py {text_file} {wav_file}" \
.venv/bin/python -u -m base_station.ws_server.server
```

OpenClaw Gateway:

```text
ws://127.0.0.1:18789
agent: xiaoan-runtime
```

Robot observed online:

```text
device_id=xiaoan_robot_01
ip=192.168.31.176
rssi=-64
firmware path=mergetesting_care_demo_face240_spoken_tts_din41_ota_usb
```

## What Passed

- Speaker hardware path produced real streamed TTS earlier in the session. The base station converts arbitrary text to PCM and sends `audio.play_tts`; the robot reported `audio.playback_done ok`.
- Streamed TTS format confirmed as `pcm_s16le`, 16000 Hz, mono.
- Late speaker bench restored the previous Demo 1 baseline: robot firmware was flashed back to `mergetesting_care_demo_face240_spoken_tts_din41_ota_usb`, base station was restarted with `XIAOAN_TTS_TARGET_PEAK=800`, and the robot reconnected after a USB reset as `192.168.31.175 -> 192.168.31.252:8765`.
- Direct streamed Chinese TTS passed through `/agent -> /control`:

```bash
.venv/bin/python tools/send_robot_command.py --url ws://127.0.0.1:8765/agent tts --text "你好，我是小安。"
```

Result:

```text
agent.ack ok=true forwarded_type=audio.play_tts
audio.playback_done status=ok bytes_written=66816 duration_ms=2999
```

- Full Demo 1 mock path passed after reconnect:

```bash
tools/demo/run_demo1_mic_to_robot.sh --once --no-screen --mock-text "小安我有点累了，出来陪我一下"
```

Result:

```text
OpenClaw returned xiaoan.robot.care
motion.execute move_out_of_dock -> ok -> motion.completed success
motion.execute turn -> ok -> motion.completed success
display.expression caring -> ok
audio.play_tts -> accepted -> audio.playback_done ok bytes_written=159744 duration_ms=5519
```

- Hardware isolation notes:
  - `mergetesting_speaker_volume_probe_din41` produced audible 700 Hz probe tones at amplitudes `2000`, `8000`, `16000`, `28000`; user reported only the final `28000` level distorted/noisy.
  - `embedded_tts_phrase.h` PCM stats: raw peak `900`, RMS `114.4`, leading quiet frames `1620`. Gain 64 clips only about `0.74%` of frames and was audible to the user.
  - `mergetesting_speaker_auto_tts_din41_gain64` USB upload PASS and user confirmed the embedded phrase was audible.
- Direct `/agent` expression probe passed:

```bash
.venv/bin/python tools/send_robot_command.py --url ws://127.0.0.1:8765/agent expression caring --duration-ms 1000
```

Result:

```text
agent.ack ok=true device_id=xiaoan_robot_01 forwarded_type=display.expression
```

- `tools/demo/run_demo1_mic_to_robot.sh --once --no-screen --mock-text "小安我累了"` reached the real OpenClaw Gateway and returned:

```text
handled=true
reply_text="我在呢。你先歇一小会儿，我出来陪你。"
tool_call=xiaoan.robot.care
```

- Server logs showed the intended serialized robot command order:

```text
motion.execute move_out_of_dock -> command.ack ok -> motion.completed success
motion.execute turn -> command.ack ok -> motion.completed success
display.expression caring -> command.ack ok
audio.play_tts -> command.ack accepted -> audio.playback_done ok bytes_written=135168 duration_ms=4776
```

## Code Changes Made

- `agent/skills/robot_motion.py`
  - `xiaoan.robot.care` now runs sequentially:
    `move_out_of_dock -> turn_left -> caring expression -> streamed TTS`.
  - It no longer uses local `care_01`.
- `agent/core/action_executor.py`
  - `reply_text` is not spoken before `xiaoan.robot.care`.
  - `reply_text` from the care tool arguments is passed into the care sequence.
- `agent/core/gateway.py`
  - `audio.play_tts` now has a longer ack wait window because the server may spend several seconds generating TTS PCM before returning `accepted`.

## Tests Run

```bash
.venv/bin/python -m unittest tests.unit.test_robot_motion_skill tests.unit.test_action_executor tests.integration.test_robot_motion_skill
```

Result:

```text
Ran 62 tests
OK
```

After the TTS ack timeout fix:

```bash
.venv/bin/python -m unittest tests.integration.test_agent_gateway tests.unit.test_robot_motion_skill tests.unit.test_action_executor tests.integration.test_robot_motion_skill
```

Result:

```text
Ran 66 tests
OK
```

## Remaining Issues

- User confirmed the restored Demo 1 TTS is audible, but still too quiet for a reliable presentation room demo.
- Keep the current fallback baseline at `XIAOAN_TTS_TARGET_PEAK=800` + ESP stream gain `32` because it avoids the earlier all-noise failure and completes `audio.playback_done ok`.
- Next speaker pass should increase effective spoken volume conservatively. Known boundary: probe amplitude `16000` is acceptable, while `28000` introduces audible distortion/noise. A practical next test is either base-station target peak `1000-1400` with stream gain `32`, or a dedicated stream-gain env between `32` and `64`, then verify with one short Chinese sentence and one full Demo 1 mock run.
- If robot shows only `Control disconnected, retry in 30000 ms` after restarting the base station, USB reset recovers the link; reconnect evidence was `Control connected`, `Sent device.hello`, and server-side `Robot connected: xiaoan_robot_01 ip=192.168.31.175`.
