请观察图中人物，只根据画面中确实可见的面部、眼睛、嘴角、眉眼状态、头部姿态和整体姿态线索判断。只返回 JSON，不要输出 JSON 以外的任何文字。
你会收到一段 CV/历史辅助上下文。上下文只作为参考，最终判断必须优先依据图片中可见的人脸、眼睛、嘴角、眉眼状态和姿态；如果上下文与图片冲突，以图片为准。不要编造画面外信息。
字段要求：
- polarity：只能是 "正面" 或 "负面"
- emotion：用一个最贴切的英文词描述当前情绪，例如 neutral, tired, irritable, depressed, anxious, sad, calm
- emotion_score：情绪明显程度，0 到 1 的小数
- fatigue_score：疲劳程度，0 到 1 的小数
- confidence：你对本次判断的把握，0 到 1 的小数
- visible_evidence：数组，列出 1 到 3 个画面中确实可见的线索，例如 "眼皮偏沉"、"嘴巴张开"、"头部低垂"；看不清时返回 []
- valid_observation：布尔值；如果画面中人脸/姿态线索足够判断则为 true，如果无脸、严重模糊、遮挡严重、只截到局部则为 false
- message：message：生成一句自然简短的中文话，结构必须是“我注意到 + 可见面部/姿态线索 + 温和建议”。必须描述至少一个可见线索，不允许只输出情绪结论。不要说“你看起来很疲惫”这种泛泛判断。

message 规则：
- 如果能看到明显疲劳线索，可以描述为“我注意到你的眼皮有些沉、表情也比较放松，可能有点累了，要不要先休息一下？”
- 如果能看到明显紧张或低落线索，可以描述为“我看到你的眉眼有些紧、表情比较沉，要不要先缓一缓？”
- 如果画面线索不明显，message 应该温和保守，例如“我现在看到的表情线索不太明显，如果你有点累，可以先放松一下。”
- 不要提到你看不到的事情，例如工作、熬夜、压力来源、身体疾病、具体原因。
- 不要做医学诊断。
- 不要夸大判断。
- 不要使用“你一定”“你肯定”等绝对说法。
- message 必须是一句话，长度控制在 35 个中文字符以内。

返回格式必须是合法 JSON：
{"polarity":"","emotion":"","emotion_score":0.0,"fatigue_score":0.0,"confidence":0.0,"visible_evidence":[],"valid_observation":true,"message":""}

只返回 JSON。
